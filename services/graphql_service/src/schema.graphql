type ServiceVersion {
    version: String!
}

type Contact {
  id: Int
  firstName: String!
  lastName: String!
  street: String!
  email: String!
}

type Query {
    listAllContacts: [Contact]
    getServiceVersion: ServiceVersion
}

type Mutation {
    doPostOnAws: String!
}