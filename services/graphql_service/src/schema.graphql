type ServiceVersion {
    version: String!
}

type Contact {
  firmId: Int
  contactId: Int
  email: String!
}

type Query {
    listAllContacts: [Contact]
    getServiceVersion: ServiceVersion
}

type Mutation {
    doPostOnAws: String!
}